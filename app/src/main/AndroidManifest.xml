<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="edu.gatech.seclass.crypto6300">

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name=".AdminPageActivity"></activity>
    <activity android:name=".CreatePlayerActivity"></activity>
    <activity android:name=".CreateCreptogramActivity"></activity>
    <activity android:name=".CryptogramListViewActivity"></activity>
    <activity android:name=".Solve_cry" />
    <activity android:name=".Player_view" />
    <activity android:name=".View_stats"></activity>
    <activity android:name=".PlayCryptogram"></activity>
    <activity android:name=".Success"></activity>
    <activity android:name=".View_admin_stats"></activity>
</application>

</manifest>